# Spring Boot Job Portal

A Job Portal web application built using **Spring Boot**, **JSP**, and **MVC architecture**.  
This project allows users to add job postings and view all available job listings.

--------

## 🚀 Features
- Add new job postings  
- View all job listings  
- MVC architecture (Controller, Service, Repository)  
- JSP with JSTL for views  
- In-memory data storage (no database yet)

--------

## Tech Stack
- Java  
- Spring Boot  
- Spring MVC  
- JSP & JSTL  
- Maven  

--------

## Project Structure
- `controller` – Handles HTTP requests  
- `service` – Business logic  
- `repo` – Data storage (in-memory)  
- `model` – JobPost entity  
- `views` – JSP pages  

--------

## How to Run the Project
1. Clone the repository  
   ```bash
   git clone https://github.com/Imran-BEW/spring-boot-job-portal.git
2. Open the project in Eclipse / IntelliJ as a Maven project

3. Run SpringProjApplication.java

4. Open browser and go to:

   http://localhost:8080
